import React, { Component } from 'react';

import './{{ pascalCase name }}.scss'

export interface {{ pascalCase name }}Props extends HTMLAttributes<HTMLElement> {
}

export interface {{ pascalCase name }}State {
}

export class {{ pascalCase name }} extends Component<{{ pascalCase name }}Props, {{ pascalCase name }}State> {
    constructor(props: {{ pascalCase name }}Props) {
        super(props);
        this.state = { };
    }

    componentDidMount() {
    }

    componentWillUnmount() {
    }

    render() {
        return (
            <div className="{{ kebabCase name }}-wrapper" {...this.props}>
                hello {{ pascalCase name }}
            </div>
        );
    }
}
